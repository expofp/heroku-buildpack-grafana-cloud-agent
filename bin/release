#!/bin/sh
# https://devcenter.heroku.com/articles/buildpack-api#bin-release-example
# https://devcenter.heroku.com/articles/procfile#procfile-format
cat << EOF
---
default_process_types:
  web: bin/grafana-agent -config.file grafana-agent.yml -server.http.address "0.0.0.0:${PORT}"
EOF
