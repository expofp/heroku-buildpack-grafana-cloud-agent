#!/bin/sh

BUILD_DIR="$1"

[ -z "$BUILD_DIR" ] && BUILD_DIR=`pwd`

CONFIG_FILE_PATH=config/grafana-agent.yml

echo "Detecting Grafana agent config..."
if [ -f "$CONFIG_FILE_PATH" ]; then
  echo "Agent config detected."
  exit 0
else
  echo "Missing agent config."
  echo "$CONFIG_FILE_PATH"
  exit 1
fi
